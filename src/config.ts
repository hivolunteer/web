// Define the configuration of the server 

const config = {
    // The url of the HiVolunteer backend API
    apiUrl: process.env.REACT_APP_API_URL || "http://localhost:8000",
};

export default config;
